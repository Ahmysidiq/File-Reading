# File-Reading
A Python-based system log parser designed to detect suspicious activity. It scans Linux log files for security keywords like SSH failures, root access, and system errors, providing a ranked frequency report
